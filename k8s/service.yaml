apiVersion: v1

kind: Service

metadata:
  name: myportfolio-service
  labels:
    app: myportfolio

spec:
  type: NodePort

  selector:
    app: myportfolio

  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30091

