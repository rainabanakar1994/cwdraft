apiVersion: v1

kind: Service

metadata:
  name: portfolio-service
  labels:
    app: portfolio

spec:
  type: NodePort

  selector:
    app: portfolio

  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30081

