apiVersion: apps/v1

kind: Deployment

metadata:
  name: portfolio
  labels:
    app: portfolio
    environment: production

spec:

  replicas: 2

  selector:
    matchLabels:
      app: portfolio

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1

  template:
    metadata:
      labels:
        app: portfolio
    spec:
      containers:
        - name: portfolio
          image: raina1994/portfolio:latest
          imagePullPolicy: Always

          ports:
            - containerPort: 8081
              protocol: TCP

          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"

          livenessProbe:
            httpGet:
              path: /
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10

